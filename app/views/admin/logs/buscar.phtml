<?php 
// Esta vista reutiliza index.phtml pero con datos de búsqueda
// Podemos hacer un partial o simplemente usar index.phtml con variables adicionales
?>

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="h3 mb-0">
            <i class="fas fa-search text-primary"></i> Búsqueda de Logs
        </h1>
        <a href="<?= PUBLIC_PATH ?>logs" class="btn btn-outline-secondary">
            <i class="fas fa-list"></i> Ver todos
        </a>
    </div>
    
    <!-- Formulario de búsqueda -->
    <div class="card mb-4">
        <div class="card-header bg-light">
            <h5 class="mb-0">Criterios de Búsqueda</h5>
        </div>
        <div class="card-body">
            <?= Form::open('logs/buscar', 'get', 'class="row g-3"') ?>
            <div class="col-md-8">
                <?= Form::label('Buscar término', 'q', 'class="form-label"') ?>
                <?= Form::text('q', 'class="form-control" placeholder="Buscar en acciones, IP, detalles..."', 
                               $termino_busqueda ?? '') ?>
            </div>
            <div class="col-md-4">
                <?= Form::label('Tipo', 'tipo', 'class="form-label"') ?>
                <?= Form::select('tipo', [
                    'todos' => 'Todos los logs',
                    'exitosos' => 'Accesos exitosos',
                    'fallidos' => 'Accesos fallidos'
                ], 'class="form-select"', $tipo_busqueda ?? 'todos') ?>
            </div>
            <div class="col-12">
                <button type="submit" class="btn btn-primary">
                    <i class="fas fa-search"></i> Buscar
                </button>
                <a href="<?= PUBLIC_PATH ?>logs" class="btn btn-outline-secondary">
                    <i class="fas fa-times"></i> Limpiar
                </a>
            </div>
            <?= Form::close() ?>
        </div>
    </div>
    
    <!-- Resultados -->
    <?php if (isset($termino_busqueda) || isset($tipo_busqueda)): ?>
    <div class="alert alert-info">
        <i class="fas fa-info-circle"></i> 
        Resultados de búsqueda: 
        <strong><?= $total_resultados ?? 0 ?></strong> registros encontrados
        <?php if (isset($termino_busqueda) && $termino_busqueda): ?>
            para "<strong><?= h($termino_busqueda) ?></strong>"
        <?php endif; ?>
        <?php if (isset($tipo_busqueda) && $tipo_busqueda != 'todos'): ?>
            (Tipo: <strong><?= $tipo_busqueda ?></strong>)
        <?php endif; ?>
    </div>
    <?php endif; ?>
    
    <!-- Incluir la vista de lista (index.phtml) -->
    <?php View::partial('logs/index') ?>
</div>