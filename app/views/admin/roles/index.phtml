<div class="container-fluid">
    <div class="page-header">
        <h1>Gestión de Roles</h1>
        <p class="lead">Administre los roles y sus permisos en el sistema</p>
    </div>
    
    <?php View::partial('flash') ?>
    
    <div class="row mb-3">
        <div class="col-md-12">
            <div class="pull-right">
                <?php if ($has_permission('roles', 'create')): ?>
                <a href="<?= PUBLIC_PATH ?>roles/crear" class="btn btn-primary">
                    <i class="glyphicon glyphicon-plus"></i> Nuevo Rol
                </a>
                <?php endif; ?>
            </div>
        </div>
    </div>
    
    <div class="row">
        <div class="col-md-12">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <h3 class="panel-title">Lista de Roles</h3>
                </div>
                <div class="panel-body">
                    <?php if (empty($roles)): ?>
                        <div class="alert alert-info">
                            No hay roles registrados en el sistema.
                        </div>
                    <?php else: ?>
                        <div class="table-responsive">
                            <table class="table table-striped table-hover">
                                <thead>
                                    <tr>
                                        <th>ID</th>
                                        <th>Código</th>
                                        <th>Nombre</th>
                                        <th>Nivel</th>
                                        <th>Permisos</th>
                                        <th>Estado</th>
                                        <th>Acciones</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <?php foreach ($roles as $rol): ?>
                                    <tr>
                                        <td><?= $rol['id'] ?></td>
                                        <td>
                                            <code><?= htmlspecialchars($rol['codigo']) ?></code>
                                        </td>
                                        <td>
                                            <strong><?= htmlspecialchars($rol['nombre']) ?></strong>
                                            <?php if ($rol['descripcion']): ?>
                                                <br>
                                                <small class="text-muted"><?= htmlspecialchars($rol['descripcion']) ?></small>
                                            <?php endif; ?>
                                        </td>
                                        <td>
                                            <span class="badge"><?= $rol['nivel'] ?></span>
                                        </td>
                                        <td>
                                            <?php if (isset($rol['total_permisos']) && $rol['total_permisos'] > 0): ?>
                                                <span class="label label-success"><?= $rol['total_permisos'] ?> permisos</span>
                                            <?php else: ?>
                                                <span class="label label-default">Sin permisos</span>
                                            <?php endif; ?>
                                        </td>
                                        <td>
                                            <?php if ($rol['activo']): ?>
                                                <span class="label label-success">Activo</span>
                                            <?php else: ?>
                                                <span class="label label-danger">Inactivo</span>
                                            <?php endif; ?>
                                        </td>
                                        <td>
                                            <div class="btn-group btn-group-xs">
                                                <a href="<?= PUBLIC_PATH ?>roles/ver/<?= $rol['id'] ?>" 
                                                   class="btn btn-default" title="Ver detalles">
                                                    <i class="glyphicon glyphicon-eye-open"></i>
                                                </a>
                                                
                                                <?php if ($has_permission('roles', 'edit')): ?>
                                                <a href="<?= PUBLIC_PATH ?>roles/editar/<?= $rol['id'] ?>" 
                                                   class="btn btn-primary" title="Editar">
                                                    <i class="glyphicon glyphicon-edit"></i>
                                                </a>
                                                <?php endif; ?>
                                                
                                                <?php if ($has_permission('roles', 'assign_permissions')): ?>
                                                <a href="<?= PUBLIC_PATH ?>roles/permisos/<?= $rol['id'] ?>" 
                                                   class="btn btn-info" title="Asignar permisos">
                                                    <i class="glyphicon glyphicon-lock"></i>
                                                </a>
                                                <?php endif; ?>
                                                
                                                <?php if ($has_permission('roles', 'delete') && $rol['codigo'] != 'admin'): ?>
                                                <a href="<?= PUBLIC_PATH ?>roles/eliminar/<?= $rol['id'] ?>" 
                                                   class="btn btn-danger" title="Eliminar">
                                                    <i class="glyphicon glyphicon-trash"></i>
                                                </a>
                                                <?php endif; ?>
                                            </div>
                                        </td>
                                    </tr>
                                    <?php endforeach; ?>
                                </tbody>
                            </table>
                        </div>
                    <?php endif; ?>
                </div>
            </div>
        </div>
    </div>
    
    <?php if (!empty($estadisticas)): ?>
    <div class="row">
        <div class="col-md-12">
            <div class="panel panel-info">
                <div class="panel-heading">
                    <h3 class="panel-title">Estadísticas de Usuarios por Rol</h3>
                </div>
                <div class="panel-body">
                    <div class="row">
                        <?php foreach ($estadisticas as $estadistica): ?>
                        <div class="col-md-3 col-sm-6">
                            <div class="panel panel-default text-center">
                                <div class="panel-body">
                                    <h4><?= htmlspecialchars($estadistica['nombre']) ?></h4>
                                    <h2><?= $estadistica['total_usuarios'] ?></h2>
                                    <p>usuarios</p>
                                </div>
                            </div>
                        </div>
                        <?php endforeach; ?>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <?php endif; ?>
</div>