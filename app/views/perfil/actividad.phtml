<div class="container mt-4">
    <div class="row">
        <div class="col-md-3">
            <?php View::partial('perfil/menu') ?>
        </div>
        
        <div class="col-md-9">
            <div class="card">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h4 class="mb-0">Mi Actividad Reciente</h4>
                    <small class="text-muted">Últimas 20 actividades</small>
                </div>
                
                <div class="card-body">
                    <?php if (empty($actividades)): ?>
                        <div class="text-center py-5">
                            <i class="fas fa-history fa-3x text-muted mb-3"></i>
                            <p class="text-muted">No hay actividades registradas.</p>
                        </div>
                    <?php else: ?>
                        <div class="list-group">
                            <?php foreach ($actividades as $log): ?>
                                <div class="list-group-item">
                                    <div class="d-flex w-100 justify-content-between">
                                        <h6 class="mb-1">
                                            <?php
                                            $iconos = [
                                                'login' => 'fas fa-sign-in-alt text-success',
                                                'logout' => 'fas fa-sign-out-alt text-warning',
                                                'password_cambiada' => 'fas fa-key text-info',
                                                'perfil_actualizado' => 'fas fa-user-edit text-primary'
                                            ];
                                            $icono = $iconos[$log->accion] ?? 'fas fa-circle text-secondary';
                                            ?>
                                            <i class="<?= $icono ?>"></i>
                                            <?= ucfirst(str_replace('_', ' ', $log->accion)) ?>
                                        </h6>
                                        <small class="text-muted">
                                            <?= date('d/m/Y H:i', strtotime($log->fecha)) ?>
                                        </small>
                                    </div>
                                    <p class="mb-1 small">
                                        <i class="fas fa-globe"></i> IP: <?= h($log->ip) ?>
                                        <?php if ($log->detalles): ?>
                                            <br><i class="fas fa-info-circle"></i> 
                                            <?= h($log->detalles) ?>
                                        <?php endif; ?>
                                    </p>
                                </div>
                            <?php endforeach; ?>
                        </div>
                    <?php endif; ?>
                </div>
            </div>
        </div>
    </div>
</div>